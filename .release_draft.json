{
  "version": "v0.1.7",
  "changelog": "Fixed exe not running from GitHub release (output-file instead of output-filename in release workflow). Added auto-download and install support to the updater."
}