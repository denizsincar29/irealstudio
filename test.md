# irealstudio (Rust) — тест-чеклист. Слайсы 1–13

Проверяется **полностью на Windows с NVDA**, по пунктам снизу. Демо-цифровка
грузится при старте — состояние, от которого отсчитываются «ожидания», описано
в конце файла.

То, что в кавычках — **точный текст озвучки** (NVDA говорит его через
ControllerClient; то же самое дублируется в статус-баре окна). «звук аккорда
B-7» значит произносится русское название аккорда из ru-каталога (то же, что
Дениз слышал в python-версии).

Если пункт не совпал — запиши **что нажал → что услышал вместо ожидаемого** и
иди дальше (это баг, не блокер). В конце файла — форма репорта.

## Сборка и запуск

1. Клонировать ветку `rust`: `git clone -b rust https://github.com/denizsincar29/irealstudio.git`
2. Сборка в обычном git bash — командная строка VS не нужна (терминал не важен;
   главное, чтобы rustup-тулчейн msvc и `link.exe` были в PATH).
3. `cd irealstudio`
4. `cargo build -p irealwx_ui` (первая сборка тянет и компилирует wxWidgets — 10–30 минут).
5. Запустить `target\debug\irealwx_ui.exe`. NVDA должна быть включена.

При старте слышим/видим: окно «irealstudio — Rhythm Changes (демо)», курсор на
такте 1.

## Слайс 1 — окно, меню, навигация

**Окно и меню**
1. Alt открывает менюбар. Проверь пункты: Файл, Правка, Вставка, Песня, Справка.
2. Alt+Файл → Выход закрывает программу (или просто Alt+F4).
3. Стрелки по меню и Enter работают; Esc закрывает меню.

**Озвучка и навигация по тактам** (курсор стоит на такте 1)
4. F5 — озвучить такт: «такт 1. секция A, …B-7, …E-7» (названия аккордов
   русским голосом).
5. F6 — озвучить всю цифровку: заголовок «Rhythm Changes (демо), B-, Swing» и
   все 12 тактов по одному на строку.
6. Вправо (→) — шаг по тактам: 1→2→3… до 12. Каждый шаг озвучивается.
   Влево (←) — обратно.
7. В конце (→ на 12) курсор остаётся на 12 (не уходит за конец); в начале (←
   на 1) остаётся на 1.
8. Home — на такт 1 («такт 1. секция A, …»), End — на такт 12 («такт 12. без
   аккорда»).
9. Ctrl+Alt+→ — по секциям вперёд: с такта 1 → такт 9 (секция B); Ctrl+Alt+→ ещё
   → такт 11 (снова A). Ctrl+Alt+← — обратно. (Слайс 12: секции переехали с Alt
   на Ctrl+Alt; Alt+←/→ теперь ходят по долям — см. слайс 12.)
10. Статус-бар при навигации показывает «Такт N из 12».

## Слайс 2 — новая цифровка (Ctrl+N)

11. Ctrl+N открывает форму «Новая цифровка» (NVDA объявляет диалог).
12. Порядок табов: Название → Композитор → Темп → Тональность → Лад → Стиль →
    Шаблон → ОК → Отмена.
13. Поля ввода читаются; Темп — счётчик 40–240 (стрелками меняется, NVDA
    называет значение); Тональность — выбор из 12 корней; Лад — мажор/минор;
    Стиль — список (там 50 стилей iReal); Шаблон — Без/Блюз/… (6 пунктов).
14. Esc закрывает форму без создания (озвучки «Новая цифровка» нет, документ
    не меняется).
15. Ввести Название «Тест», выбрать всё, что удобно, Enter (или ОК) → озвучка
    «Новая цифровка: Тест». Заголовок окна — «irealstudio — Тест». Курсор на
    такте 1.
16. Если задать шаблон AABA — цифровка получит 32 такта с секциями; если «Без
    шаблона» — пустая прогрессия (навигация покажет такты по мере добавления
    аккордов; см. слайс 5).

## Слайс 3 — файлы .ips (Ctrl+O / Ctrl+S / Ctrl+Shift+S)

17. На цифровке «Тест» нажми Ctrl+S → диалог «Сохранить как…» с именем по
    умолчанию `Тест.ips` (пробелы в названии → подчёркивания). Сохрани в любую
    папку → озвучка «Сохранено: Тест.ips». Звёздочка в заголовке исчезает.
18. Внеси правку (→ на такт 2, потом слайс 5, напр. Del по аккорду) — в начале
    заголовка появляется `*`. Ctrl+S повторно сохраняет **без диалога**
    (озвучка «Сохранено: …») — звёздочка гаснет.
19. Ctrl+Shift+S — всегда диалог «Сохранить как…».
20. Ctrl+O — диалог открытия; выбери только что сохранённый файл → озвучка
    «Открыто: Тест», курсор на такте 1, звёздочки нет.
21. Отмена в диалогах файлов ничего не меняет.
22. (Проверка формата файла.) Сохранённый `.ips` — JSON: открой в текстовом
    редакторе — видно название, стиль, аккорды. Файл читается и обычным
    irealstudio (python), и наоборот.

## Слайс 4 — экспорт в iReal Pro (Ctrl+E)

23. Ctrl+E → диалог сохранения, имя по умолчанию `Rhythm Changes (демо).html`
    (или имя текущей цифровки). Сохрани.
24. Озвучка «Экспортировано: …». Открой файл браузером — он должен
    переадресовать в приложение iReal Pro (на устройстве, где iReal Pro стоит)
    с открытой цифровкой.
25. Повтори с расширением `.txt` (в диалоге выбери тип «Text files»): файл
    содержит сырой irealbook-URL (для отладки, без HTML).

## Слайс 5 — редактор цифровки (главный)

Редактирование работает на **позиции под курсором = (такт, доля)**. Курсор
приходит на такт на долю 1; встать на другую долю (в т.ч. пустую) можно
Alt+←/→ (слайс 12). Правки адресуют аккорд **на доле курсора**, вставка кладёт
аккорд на долю курсора. Пункты 26–44 проверены для доли 1 (дефолт при входе в
такт); для других долей поведение то же — см. слайс 12. Все правки кладут
снимок в undo.

**Вставка и правка аккорда**
26. На такте 1 (там B-7 на доле 1, E-7 на доле 3): Ctrl+Enter → форма «Добавить
    аккорд», поле предзаполнено «B-7». Введи `C7`, Enter → озвучка «Вставлен
    аккорд: C7». На такте 1 теперь первый аккорд C7 (E-7 на доле 3 не тронут).
27. F2 на том же такте → форма «Изменить аккорд» с текущим именем `C7`.
    Поменяй на `Cmaj7`, Enter → «Аккорд отредактирован: Cmaj7».
28. F2 → подтверди без изменений (Enter) → **тишина** (ничего не меняется).
29. На пустом/без-аккорда такте (напр. такт 12, там N.C.) Ctrl+Enter вставка
    работает; F2 говорит «Нет аккорда для редактирования».
30. Ctrl+Enter с пустым полем (сразу Enter) → тишина, ничего не вставлено.
31. Ctrl+Z — отмена: возвращает предыдущее состояние, озвучка «Отменено».
    Ctrl+Y — повтор: «Повторено». На пустых стеках — «Нечего отменить» /
    «Нечего повторить».

**N.C. (клавиша N)**
32. Встань на такт 1 (там аккорды): нажми N → «N.C. в такте 1» — на такте 1
    теперь N.C. (аккорды скрыты, в сетке «N.C.»). Ещё раз N → «N.C. убрано в
    такте 1», аккорды вернулись.
33. На такте 12 (уже N.C.): N → «N.C. убрано в такте 12», ещё раз → «N.C. в
    такте 12».
34. Клавиша N с Ctrl/Alt/Shift не срабатывает как N.C. (комбинации зарезервированы).

**Удаление**
35. На такте с аккордом нажми Del (и Backspace — то же) → удаляется первый
    аккорд такта; озвучка «Удалено. <озвучка такта под курсором>». На такте, где
    это был единственный аккорд, курсор уходит на предыдущий такт с аккордом.
36. Del на такте 12 (N.C., аккордов нет) → удаляет структуру: «Удалено: N.C. в
    такте 12».
37. Ctrl+Del / Ctrl+Backspace — только структура (метка части / знак повтора /
    N.C.), аккорды не трогает. На такте 11 (метка «Часть A» + B-7): Ctrl+Del →
    «Удалено: метка части в такте 11», аккорд B-7 остался. Ещё раз Ctrl+Del →
    «Нечего удалить в такте 11 доле 1».
38. Обычный Del на такте 11 после удаления метки снова удаляет аккорд.

**Слэш-бас**
39. Меню Вставка → Басовая нота… (или пункт меню) → форма «Нота». На такте 1 с
    Cmaj7 введи `E`, Enter → озвучка русского аккорда с басом (Cmaj7/E). Такт 1
    теперь показывает Cmaj7/E.
40. Введи несуществующую ноту (напр. `H` или пусто) → «Неверная нота».
41. В демо такт 4 уже содержит D7/A (слэш на доле 4) — F6/озвучка такта 4
    произносит его с басом.

**Буфер обмена (копировать/вырезать/вставить)**
42. Встань на такт с аккордом, Ctrl+C → «Скопировано: <аккорд>». Встань на
    пустой такт (создай его через вставку в N.C.-такт, или такт без аккордов),
    Ctrl+V → «Вставлено: <аккорд>». Повторный Ctrl+V на тот же такт
    перезаписывает (буфер не очищается — как в python).
43. Ctrl+X на такте с аккордом → «Вырезано: <аккорд>» (аккорд удалён, копия в
    буфере). Ctrl+V клеит.
44. Ctrl+C на такте без аккорда → «Нет аккорда под курсором», буфер не меняется.
45. Ctrl+V при пустом буфере (сразу после старта) → «Буфер обмена пуст».

**Транспонирование (Ctrl+T)**
46. Ctrl+T → счётчик «На сколько полутонов:» (диапазон −11..+11, по умолчанию 0).
    Введи 2, Enter → «Транспонировано на 2 полутона, новая тональность: <ключ>»;
    все аккорды и ключ сдвинуты на +2. Правь против часовой (меньше 0) тоже.
47. Оставь 0 (Enter сразу) → **тишина**, ничего не меняется (и на ±12 — тоже
    тишина: знак не меняется).
48. Озвучка единицы склоняется: 1 полутон, 2/3/4 полутона, 5+ полутонов.
49. Ctrl+Z после транспонирования возвращает исходную тональность и аккорды
    («Отменено»).

**Переход к такту (Ctrl+F)**
50. Ctrl+F → счётчик «Номер такта (1–12):» (по умолчанию текущий). Введи 9,
    Enter → курсор на такте 9, озвучка «такт 9. секция B, …». Вне диапазона —
    зажимается в 1..12.

**Метки частей (Ctrl+Shift+буква)**
51. Сбрось состояние: Ctrl+N → новая цифровка «Тест2» (поле названия), ОК. Затем
    добавь такты аккордами (вставь аккорд на такты 1, 5, 9 — Ctrl+Enter на каждом).
52. На такте 1 Ctrl+Shift+A → «Часть A в такте 1». Такт 5 Ctrl+Shift+B → «Часть B
    в такте 5». Такт 9 Ctrl+Shift+A → «Часть A в такте 9».
53. Такт 11: Ctrl+Shift+V → «Куплет в такте 11»; Ctrl+Shift+I → «Вступление в
    такте …»; Ctrl+Shift+S → «Сеньо …»; Ctrl+Shift+Q → «Кода …».
54. Все метки доступны и через меню: Вставка → Метка части → (Часть A…Кода).
55. F6 читает цифровку с секциями («такт 1. секция A, …», «такт 5. секция B,
    …») — структура озвучивается.
56. Ctrl+Del на такте с меткой удаляет только метку (п. 37). После удаления
    метки озвучка такта без секции — «такт N. <аккорды>».

## Слайс 6 — настройки цифровки (Ctrl+P), выход (Ctrl+Q)

Настройки меняют только поля цифровки; смена тональности аккорды НЕ
транспонирует (как в python). Демо предзаполнено: «Rhythm Changes (демо)»,
композитор «Gershwin / Ragas», 160 BPM, 4/4, B- (си минор), Swing.

57. Ctrl+P (или меню **Настройки → Настройки цифровки…**) — открывается форма
    «Настройки цифровки». Порядок табов: Название → Композитор → Темп → Размер
    такта → Тональность → Лад → Стиль → ОК → Отмена.
58. В Названии измени текст (напр. «Моя песня»), Стиль поменяй на Bossa Nova,
    ОК (или Enter) → озвучка «Settings updated: Моя песня». В начале заголовка
    окна появляется `*` (несохранённая правка). Остальные поля не тронуты.
59. Ctrl+Z → «Отменено»: название и стиль вернулись к прежним (и `*` исчезла).
60. Ctrl+P, поменяй только Тональность на мажор того же корня (Лад → мажор),
    ОК → «Settings updated: …». F6 (озвучить всю цифровку): ключ в шапке сменился,
    а **аккорды не сдвинулись** — смена тональности не транспонирует.
61. Ctrl+P → ОК, ничего не меняя → **тишина** (без изменений, `*` не появляется).
62. Ctrl+P → Esc (или Отмена) → форма закрывается, ничего не меняется.
63. Размер такта: Ctrl+P, в поле «Размер такта» введи `3/4`, ОК → «Settings
    updated: …». Открой файл (Ctrl+Shift+S → сохрани в `.ips` и проверь, или
    прочитай через python) — размер теперь 3/4.
64. Темп — счётчик 40–240; выйти за диапазон нельзя. Композитора можно очистить
    (поле пустым) — тогда имя композитора просто не применится (титул из пустого
    поля тоже не стирается).
65. Ctrl+Q закрывает программу — то же, что Alt → Файл → Выход. Внимание:
    подтверждения «Сохранить изменения?» при закрытии ещё нет — при
    несохранённых правках программа просто закрывается (известное ограничение).
66. Меню **Настройки** стоит в менюбаре между «Песня» и «Справка» (проверь
    стрелками по меню).

## Слайс 7 — вольты и повторы (V, `[`, `]`)

Повтор задаётся парой: `[` — начало, `]` — конец (обычный повтор, без
окончаний). Если поставить курсор на такт *внутри* заданного повтора и нажать
`V` — получается вольта: окончание 1 от курсора до конца повтора, окончание 2
после скрытой части. `V` без маркеров делает вольту по границам секции (как
python). Хоткеи работают и без записи (подсистемы записи в этой версии нет).
Каждый сценарий начинается со свежего старта — перезапусти exe.

67. Такт 2, `[` → «Начало повтора задано на такте 2». Цифровка не меняется:
   F6 по-прежнему озвучивает все 12 тактов.
68. Перейти на такт 5, `]` → «Повтор задан: 2–5. Если нужны окончания, перейдите
   к окончанию 1 и нажмите V.» Аккорды не удаляются.
69. Отмена/повтор работают на созданном повторе: Ctrl+Z → «Отменено», повтор
   снят; Ctrl+Y → «Повторено», повтор вернулся. Пункт меню «Вставка → Вольта /
   Окончание» (хоткей V) есть.
70. Свежий старт. Такт 5, `]` (начала не задавали) → «Сначала задайте начало
   повтора клавишей [». Повтор не создаётся.
71. Такт 6 `[`, вернуться на такт 3, `]` → «Конец повтора должен быть после
   начала». Повтор не создаётся.
72. Свежий старт. Такт 2 `[`; такт 7 `]` → «Повтор задан: 2–7. …»; перейти на
   такт 4 и нажать `V` → «Реприза с такта 2, вольта 1: 4–7, вольта 2 начинается
   с такта 10 (удалено 4 скрытых аккорда)». Скрытые такты 8–9 опустели: F6 больше
   не озвучивает там F7 B-7 / F7 F7.
73. (продолжение п.72) Ctrl+Z → «Отменено», скрытые аккорды вернулись (F6: такты
   8–9 снова с аккордами). Ctrl+Y → «Повторено», снова скрыты.
74. Свежий старт. Такт 2 `[`; такт 7 `]`; перейти на такт 8 (за концом повтора),
   `V` → «Неверные маркеры вольты: начало 2, вольта 8, конец 7». Ничего не
   удаляется, повтор 2–7 остаётся.
75. Свежий старт. Перейти на такт 10 (секция B: End → Alt+→ к B → →), `V` без
   маркеров → «Реприза с такта 9, вольта 1: 10–10, вольта 2 начинается с такта
   12 (удалён 1 скрытый аккорд)». Скрытый такт 11 пустеет (B-7 исчез из F6).

## Слайс 8 — подтверждение несохранённых изменений

Закрытие окна (крестик / Alt+F4) и «Выход» (Файл → Выход или Ctrl+Q), а также
«Новая цифровка» (Ctrl+N) при несохранённых правках спрашивают подтверждение —
как python. Диалог: заголовок **Несохранённые изменения**, кнопки Сохранить /
Не сохранять / Отмена. Enter = Сохранить (дефолт), Esc = Отмена. Если документ
чистый — вопрос не задаётся вовсе.

Чтобы «испачкать» документ: встань на такт 1 и нажми Del — аккорд B-7 удаляется
и в заголовке окна появляется звёздочка (признак несохранённых правок). Сценарии
начинаются со свежего старта (перезапусти exe), кроме помеченных.

76. Такт 1, Del — в заголовке «*». Ctrl+Q → диалог ««Rhythm Changes (демо)»
   содержит несохранённые изменения.

   Сохранить перед закрытием?» (заголовок окна диалога «Несохранённые
   изменения»). Esc → диалог закрылся, окно осталось, звёздочка на месте.
77. (продолжение п.76) Ctrl+Q снова → жми «Не сохранять» → программа закрылась.
78. Свежий старт **без правок**: Ctrl+Q → закрывается сразу, без диалога. То же
   Alt+F4 → без диалога. (Вопрос не должен надоедать на чистом документе.)
79. Свежий старт. Такт 1, Del. Alt+F4 (или крестик мышью) → тот же диалог
   «…Сохранить перед закрытием?». Esc → окно осталось. Alt+F4 → «Не сохранять»
   → программа закрылась.
80. Сохранение при выходе в уже сохранённый файл. Свежий старт. Ctrl+S →
   «Сохранить цифровку как», набери имя `guard1.ips`, Сохранить → «Сохранено:
   guard1.ips», звёздочки нет. Такт 1, Del (звёздочка). Ctrl+Q → диалог →
   «Сохранить» → «Сохранено: guard1.ips», программа закрылась. Перезапусти,
   Ctrl+O → открой `guard1.ips` → звёздочки нет, и такт 1 снова без B-7
   (удаление сохранилось: F6 первым в такте 1 уже не называет B-7).
81. «Сохранить как» при выходе отменено — выход отменяется. Свежий старт. Такт 1,
   Del. Ctrl+Q → «Сохранить» → файла ещё нет, открылся «Сохранить цифровку как»
   → Отмена → вернулись в окно, звёздочка осталась, программа НЕ закрылась.
   Ctrl+Q → «Не сохранять» → закрылась.
82. Ctrl+N при грязи — Отмена и «Не сохранять» не теряют документ. Свежий старт.
   Такт 1, Del. Ctrl+N → диалог «…Сохранить перед созданием новой цифровки?».
   Esc → форма «Новая цифровка» не открылась, правки на месте. Ctrl+N → «Не
   сохранять» → открылась форма «Новая цифровка» → Esc (отмена формы) → старый
   документ остался: такты прежние, звёздочка есть.
83. Ctrl+N → «Сохранить», но «Сохранить как» отменён → новая не создаётся.
   Свежий старт. Такт 1, Del. Ctrl+N → «Сохранить» → файла нет, открылся
   «Сохранить цифровку как» → Отмена → форма новой цифровки не открылась,
   звёздочка осталась, документ на месте.
84. Ctrl+N с сохранением в файл → создаётся новая. Свежий старт. Ctrl+S → набери
   `guard2.ips`, Сохранить → «Сохранено: guard2.ips». Такт 1, Del. Ctrl+N →
   «Сохранить» → сразу «Сохранено: guard2.ips» (без диалога — файл уже есть) →
   открылась форма «Новая цифровка». Название: `Slice8`, ОК → «Новая цифровка:
   Slice8», звёздочки нет, это новый документ (не «Rhythm Changes»).

## Слайс 10 — дебаг-клавиша D: проверка озвучки NVDA

Дениз положил `nvdaControllerClient.dll` рядом с exe, а озвучка молчит.
Слайс учит крейт речи сообщать причину и добавляет клавишу D: она проговаривает
контрольную фразу тем же путём, что и обычные объявления, а если не вышло —
пишет причину в консоль, в статус-строку и в файл `irealwx_speech_debug.txt`
рядом с exe (если рядом писать нельзя — в системный temp).

88. Нажми D на любой цифровке → NVDA проговорила «Проверка озвучки…» и текущий
   такт. Цифровка при этом не меняется: звёздочки «*» в заголовке нет, ничего
   не правится.
89. Если D молчит: открой `irealwx_speech_debug.txt` рядом с exe (или в %TEMP%)
   — там причина одной строкой. Возможные варианты и что они значат:
   - «DLL не найдена. Искал: …» — файла нет ни рядом с exe, ни в каталогах NVDA.
   - «DLL есть на диске, но не загрузилась… код ошибки …» — почти всегда
     разрядность DLL (32/64) не совпадает с разрядностью exe (код 193 = не та
     битность).
   - «нет экспорта nvdaController_speakText» — положил не ту DLL.
   - «вернул код … — обычно NVDA не запущен» — NVDA выключен в момент нажатия.
90. Та же причина показана в статус-строке окна; при запуске из консоли строка
   `[debug] …` печатается и туда.
91. F1 → в конце списка появился блок «Отладка» с описанием клавиши D.

## Слайс 9 — справка по клавишам (F1)

Справка → «Клавиатурные сокращения», а также просто F1 — модальный диалог со
списком всех горячих клавиш. Аддитивный слайс: добавлен только пункт меню и
диалог, поведение слайсов 1–8 не менялось (проверять 1–84 не заново).

85. F1 → открылся диалог «Клавиатурные сокращения — irealstudio», NVDA сразу
   прочитала текст с начала. Стрелками листается весь список; Tab → кнопка
   «ОК», Enter (или ОК) закрывает диалог — возврат в окно цифровки.
86. Справка → «Клавиатурные сокращения» — тот же диалог. Esc в диалоге не
   обязателен (кнопки «Отмена» нет — как в python).
87. Сверка списка с реальными клавишами: в диалоге есть Ctrl+N, Ctrl+O, Ctrl+S,
   Ctrl+E, Ctrl+Q, Ctrl+Z/Y, Ctrl+X/C/V, Ctrl+T, Ctrl+Enter, F2, N, V, [, ],
   Ctrl+Shift+A/B/C/D/V/I/S/Q, F5, F6, Ctrl+F, Ctrl+P. Живьём проверь пару
   ключевых, чтобы текст не разошёлся с кодом: Ctrl+Enter предлагает аккорд,
   F2 — правку аккорда под курсором, Ctrl+Shift+I ставит метку «Вступление».

## Слайс 11 — навигация MuseScore, NVDA-метки форм, Ctrl+Shift+S, Ctrl+W

Три фикса по фидбеку Дениза (1585/1587/1589) + уточнения 1592/1594.
Демо-цифровка: в тактах секции A по два аккорда — на доле 1 и на доле 3
(напр. такт 1: B-7, E-7). На этом «два шага на такт» удобно сверять стрелки.

92. Озвучка внутритактового шага — аккорд первым, БЕЗ запятых (слайс 12,
   msg 1598). Home (встань на такт 1). Нажми → → NVDA: «E-7 такт 1 доля 3».
   Первая стрелка идёт на ВТОРОЙ аккорд того же такта, а не в такт 2. Формат
   позиции везде — «аккорд такт N доля M».
93. → ещё раз → сменился такт, NVDA озвучивает элемент НА НОВОЙ ПОЗИЦИИ, а не
   весь такт (слайс 13, msg 1602: «текущий элемент, а не блин весь такт»):
   «A-7 такт 2 доля 1». Ещё два → — такт 3 и такт 4 (элемент на доле 1). Стрелки
   шагают по аккордам (по два шага на такт демо), а не по тактам.
94. ← назад — по тем же аккордам в обратную сторону: из такта N доли 3 первая
   ← возвращает на долю 1 того же такта, вторая — на долю 3 предыдущего такта.
95. Пустые такты в середине перешагиваются. Убери все аккорды из одного такта
   в середине (Del по каждому аккорду — такт остаётся, но становится пустым;
   на экране «—»). Встань на предыдущий такт и жми →: пустой такт НЕ
   останавливает — озвучен аккорд ПОСЛЕ него. Стрелкой «в пустой такт середины»
   встать нельзя (она идёт по аккордам).
96. Пустые такты в конце — шаг «в такт». Удали аккорды последних двух тактов.
   С последнего аккорда песни жми →: курсор идёт на пустые такты по одному
   (озвучка «такт N доля 1 пусто» — элемент, слайс 13), до конца песни; на
   последнем такте → молчит.
97. Границы беззвучны: ← на такте 1 и → на последнем такте ничего не делают
   (нет зацикленной озвучки, курсор не ходит по кругу).
98. Ctrl+→ / Ctrl+← — по тактам ВКЛЮЧАЯ пустые, курсор всегда на доле 1,
   озвучка — весь такт целиком. В пустой такт середины Ctrl+→ встаёт
   («такт N. пустой такт»). (В демо Ctrl+→ = с такта 1 сразу такт 2.)
99. Навигация не правит документ: после всех стрелок звёздочки «*» в заголовке
   нет, Ctrl+Z нечего отменять. Home/End, F5/F6 работают как раньше. В
   статус-строке после каждого шага «Такт X из Y». (Стрелки Alt и Ctrl+Alt —
   перераспределены в слайсе 12, см. ниже.)
100. NVDA-имена в форме «Новая цифровка» (Ctrl+N). Tab по полям — NVDA
    озвучивает имя каждого поля: «Название», «Композитор», «Темп (BPM)»,
    «Тональность», «Лад», «Стиль», «Шаблон» (раньше читалось «Редактор»,
    «Комбобокс» — без имён). Комбобоксы (Тональность, Лад, Стиль, Шаблон)
    открываются Alt+стрелка вниз и читают варианты.
101. Те же имена в «Настройки цифровки» (Ctrl+P): Название, Композитор, Темп
    (BPM), Размер такта, Тональность, Лад, Стиль.
102. Мелкие диалоги с одним полем подписаны так же: Ctrl+Enter и F2 — поле
    «Аккорд», Ctrl+T — «На сколько полутонов», Ctrl+F — «Номер такта (1–Y)»,
    «Басовая нота». Поле читается с именем.
103. Вопрос о несохранённых правках — теперь текст диалога, а не поле. Сделай
    правку и закрой окно (Ctrl+W, крестик или Alt+F4): NVDA читает вопрос
    «…содержит несохранённые изменения…» как текст диалога и кнопки
    «Сохранить», «Не сохранять», «Отмена»; текст вопроса редактировать нельзя.
    Enter = Сохранить, Esc = Отмена (возврат в окно). То же при Ctrl+N, когда
    есть несохранённые правки.
104. Ctrl+Shift+S — «Сохранить как» (диалог выбора файла), а не метка «Сеньо».
    У цифровки без файла Ctrl+S ведёт туда же. «Сеньо» осталась в
    «Вставка → Метка части» (без хоткея) — проверь, что пункт там есть.
105. Ctrl+Shift+Q по-прежнему ставит «Кода»; Части A/B/C/D, Куплет, Вступление —
    как раньше (метки частей не сломались).
106. Ctrl+W — закрывает окно: без правок сразу, с правками — вопрос из п.103.
    Ctrl+Q — выход (тоже через подтверждение). После «Отмена» окно остаётся,
    цифровка не тронута.
107. F1 — справка по клавишам обновлена: Ctrl+Shift+S — сохранить как, Ctrl+W —
    закрыть окно; в «Песня» — ←/→ по аккордам и пустым тактам, Ctrl+←/→ по
    тактам; в «Вставка» — Сеньо без хоткея, Ctrl+Shift+Q — Кода.

## Слайс 12 — позиция «аккорд такт N доля M», правка по доле, стрелки, метки полей

Четыре фикса по фидбеку Дениза (msg 1598). Что изменилось против слайсов 1–11:

- Озвучка позиции — «аккорд такт N доля M» (аккорд первым, БЕЗ запятых), напр.
  «Ми бемоль 7 такт 1 доля 1». Старого «такт N, доля M, аккорд» больше нет.
- Правки и буфер адресуют аккорд **на доле курсора**: F2, Ctrl+C/X, Del ищут
  аккорд именно на доле, где стоит курсор (второй аккорд такта правится на
  месте), а Ctrl+Enter / Ctrl+V кладут аккорд на долю курсора — в т.ч. пустую.
- Стрелки перераспределены: Alt+←/→ — по долям внутри/через такты (как MuseScore,
  встаёт и на пустую долю); секции переехали на Ctrl+Alt+←/→.
- Метки полей в диалогах: метка (StaticText) теперь создаётся ДО поля — NVDA
  связывает их по порядку создания и озвучивает имя поля (раньше читала просто
  «Редактор»/«Комбобокс»).

Сверка (демо: такт 1 — B-7 на доле 1, E-7 на доле 3; размер 4/4):

108. Формат озвучки. Home (такт 1, доля 1). Нажми → : NVDA «E-7 такт 1 доля 3»
    (аккорд первым, запятых нет). Вставь через Ctrl+Enter аккорд с «бемоль» в
    имени (напр. Bb7) на свободную долю и Alt-стрелками встань на него: озвучка
    «Си-бемоль 7 такт N доля M» — так же, аккорд первым, без запятых.
109. Alt+→ по долям внутри такта. Home. Alt+→ → «такт 1 доля 2 пусто» (доля 2
    свободна). Alt+→ → «E-7 такт 1 доля 3». Alt+→ → «такт 1 доля 4 пусто».
    Alt+→ ещё раз → «A-7 такт 2 доля 1» (с последней доли — доля 1 следующего
    такта). Alt+← обратно: «такт 1 доля 4 пусто» → «E-7 такт 1 доля 3» → «такт 1
    доля 2 пусто» → «B-7 такт 1 доля 1». На «такт 1 доля 1» Alt+← молчит
    (граница документа).
110. Вставка на пустую долю. Home, Alt+→ (доля 2 пусто), Ctrl+Enter → «Добавить
    аккорд», введи C7, Enter → «Вставлен аккорд: C7». Проверь Alt+←/→: на доле 2
    теперь «C7 такт 1 доля 2», соседние B-7 (д.1) и E-7 (д.3) на месте. (Раньше
    Ctrl+Enter клал только в долю 1.)
111. F2 правит аккорд на доле курсора, а не первый в такте. Встань на E-7 (Home,
    Alt+→, Alt+→ — доля 3). F2 → «Изменить аккорд» с именем `E-7`. Поменяй на
    `F#-7`, Enter → «Аккорд отредактирован: F#-7». F5 (такт 1): «… B-7 … F#-7 …»
    — B-7 на доле 1 НЕ тронут, правка легла на долю 3.
112. Копия/вставка по доле. На F#-7 (доля 3, из п.111) Ctrl+C → «Скопировано:
    F#-7» (не B-7). Alt+→ (доля 4 пусто), Ctrl+V → «Вставлено: F#-7» — на доле 4.
113. Del — по доле курсора, чужие аккорды не трогает. На F#-7 доли 4 (из п.112)
    Del → «Удалено…»; B-7 (д.1), C7 (д.2, из п.110), F#-7 (д.3) остались; курсор
    встал на первый аккорд такта. Del на пустой доле (Alt+→ на д.4) не «съедает»
    аккорды других долей — делает только структурное действие, как п.35–36.
114. Секции — Ctrl+Alt+←/→. Home: Ctrl+Alt+→ → такт 9 целиком («такт 9. секция B,
    …»); Ctrl+Alt+→ → такт 11 (A); Ctrl+Alt+← → такт 9. Alt+←/→ при этом ходят
    по долям (п.109), а не по секциям.
115. Метки полей в «Новой цифровке» (Ctrl+N) реально озвучиваются. Tab по полям:
    NVDA читает «Название», «Композитор», «Темп (BPM)», «Тональность», «Лад»,
    «Стиль», «Шаблон» — а не «Редактор»/«Комбобокс». Esc. Ctrl+P → «Настройки
    цифровки»: те же имена + «Размер такта».
116. Мелкие диалоги подписаны: Ctrl+Enter / F2 — поле «Аккорд», Ctrl+T — «На
    сколько полутонов», Ctrl+F — «Номер такта (1–12)», Вставка → Басовая нота —
    «Нота (например, E)». NVDA озвучивает имя поля до ввода.
117. F1 — справка отражает новые стрелки: в «Песня» — Alt+←/→ по долям (вставка
    на пустую долю — Ctrl+Enter), Ctrl+Alt+←/→ по секциям; в «Вставка» — F2
    правит аккорд под курсором на его доле.

## Слайс 13 — шаг на новый такт = элемент; аккорд с «битым» именем не роняет речь

Два фикса по фидбеку Дениза (msg 1602/1603). Что изменилось против слайса 12:

- Простая стрелка (и любой шаг по событиям) при переходе на НОВЫЙ такт озвучивает
  текущий элемент «аккорд такт N доля M», а не весь такт целиком. Раньше
  перешагивание границы такта вычитывало весь такт (секцию и все аккорды) —
  Дениз: «пусть озвучивает так же текущий элемент». Секции по-прежнему целиком
  читает только Ctrl+Alt+←/→ (п.114).
- Ввод аккорда с нераспознанным корнем больше НЕ роняет речь. Паника была в
  spoken.rs: имя без распознанного корня (напр. «C6», набранное в русской
  раскладке — кириллическая «С») давало пустой корень, и озвучка падала с
  «byte index 1 is out of bounds for string of length 0» — говорилка замолкала.
  Теперь такое имя хранится и читается как есть (что набрал — то и слышишь).

Сверка (демо: такт 1 — B-7 на доле 1, E-7 на доле 3; такт 2 — A-7 на доле 1):

118. Шаг на новый такт. Home (такт 1, доля 1). → → → : после первого → «E-7 такт 1
    доля 3» (внутри такта), после второго → «A-7 такт 2 доля 1» — ЭЛЕМЕНТ нового
    такта, не «такт 2. …» целиком. Ctrl+Alt+→ при этом по-прежнему читает такт
    целиком («такт 9. секция B, …»).
119. Битый аккорд не убивает речь. Ctrl+Enter, введи заведомо несуществующий
    аккорд на русской раскладке (напр. «С6» — кириллическая С, латинская C на
    русской раскладке), Enter. Озвучка такта продолжает работать: имя читается
    как набрано («С6»), говорилка НЕ молчит, приложение не паникует. Дальше
    можно нормально править такт (Del по этому аккорду — убрать).
    ВНИМАНИЕ: с этого слайса (п.120) ввод через диалог «Добавить аккорд» такой
    мусор ОТВЕРГАЕТ — ошибкой. П.119 про то, что ОТКРЫТЫЙ/загруженный мусорный
    аккорд (например, старый файл) озвучку не роняет.

## Слайс 14 — строгий ввод аккорда, навигация и удаление, Shift-выделение

Семь правок по фидбеку Дениза (msg 1607) + «выделить всё» из аудита python.
Демо по-прежнему «Rhythm Changes»: 12 тактов; хвост (такт 12) пустой (N.C.);
такт 4: F7 на доле 1 и D7/A на доле 4 (слэш-бас); такт 1: B-7 на доле 1, E-7
на доле 3.

**Строгий ввод (корень + функция должны существовать в iReal Pro):**
120. Несуществующий аккорд отвергается. Ctrl+Enter («Добавить аккорд»), введи
    `Cq7` или `H7`, Enter. Ожидай: «Ошибка: аккорд «Cq7» не существует в iReal
    Pro», аккорд НЕ вставлен (озвучь такт — прежнее содержимое). Так же
    кириллица: введи «С6» (русская раскладка) — «Ошибка: …», не вставляется.
121. Варианты записи нормируются. Ctrl+Enter, введи `B-7`, Enter — ожидай
    «Вставлен аккорд: Bm7» (минус-семёрка хранится как каноническое m7), озвучка
    «си минор 7». Введи `C^7` → хранится `Cmaj7` (мажор). Отмена/пусто — молчание.
122. Слэш-бас в вставке. Ctrl+Enter, введи `Bb7/G`, Enter — «Вставлен аккорд:
    Bb7/G». Введи с несуществующей нотой (`C/X` или `C/G7`) — «Ошибка: после /
    не нота или такой ноты не существует».

**Стрелка влево и пустой хвост (было: прыжок к последнему аккорду):**
123. ← из пустого хвоста — шаг по такту. Дойди стрелкой → по пустым тактам в
    конец (такт 12, «нет аккордов»). Теперь ← один раз — такт 11 (не прыжок к
    последнему аккорду где-то в середине). Ещё ← — предыдущий такт/аккорд.

**Удаление оставляет курсор на доле:**
124. Del не сдвигает в долю 1. Демо такт 1, доля 3 (E-7). Alt+→ до доли 3, Del.
    Курсор ОСТАЁТСЯ в такте 1 на доле 3 (пустота на месте удалённого), а не
    прыгает в долю 1. Повторный Del/Backspace на пустой доле — «Нет аккорда…»,
    курсор не двигается.

**N.C. только ставится (N), убирается Backspace:**
125. N больше не снимает N.C. Встань на такт 12 (N.C.). Нажми N — ожидай «N.C.
    уже стоит в такте 12» (не убирается). Backspace — N.C. снята. Нажми N на
    пустом такте — «Без аккорда (N.C.) в такте N»; ещё N — снова «уже стоит».

**F2 — полный символ с басом + валидация баса:**
126. F2 показывает и бас. Демо такт 4, доля 4 (D7/A): встань на D7/A, F2 — поле
    содержит `D7/A` (раньше было `D7`). Убери `/A`, Enter — бас снят (такт теперь
    D7). Верни бас: F2, впиши `D7/Eb`, Enter — «D7/Eb».
127. F2 с неверной нотой. F2 на аккорде, в поле впиши `D7/X` или `D7/H`, Enter —
    «Ошибка: …», аккорд не меняется.

**«/» открывает диалог басовой ноты:**
128. Клавиша «/». Встань на аккорд (напр. B-7, такт 1 доля 1), нажми «/» — диалог
    «Басовая нота». Введи `e` → аккорд стал `Bm7/E`, озвучка «си минор 7 над ми».
    Введи `X` — «Неверная нота», аккорд не меняется.

**Shift-выделение озвучивает и копирует весь диапазон:**
129. Озвучка диапазона. Демо такт 1 доля 1 (B-7). Shift+→ — «E-7 — выделено:
    2 аккорда». Shift+→ ещё — «A-7 такт 2 … выделено: 3 аккорда». Shift+← —
    диапазон сужается, число пересчитывается.
130. Ctrl+C копирует весь диапазон. Выдели 3+ аккорда (п.129), Ctrl+C — «Скопировано:
    N аккордов» (а не только первый). Ctrl+V на пустой такт — блок вставляется
    целиком («Вставлено: N аккордов»), внутренняя геометрия (пустые такты между
    аккордами) сохраняется.
131. Ctrl+X/Del на выделении. Выдели диапазон, Ctrl+X — «Вырезано: N аккордов»
    (курсор — на последний аккорд перед диапазоном). Выдели, Del — «Удалено: N
    аккордов».
132. «Выделить всё» (Ctrl+A). Нажми Ctrl+A — «… выделено: N аккордов» (все
    аккорды цифровки), курсор в конце. Ctrl+C — «Скопировано: N аккордов».
    На пустой цифровке Ctrl+A — «Нет аккордов для выделения».
133. Справка (F1) обновлена: в списке есть строки про Shift+←/→ (выделение),
    «/» басовую ноту, Ctrl+A.

## Состояние демо при старте (для сверки ожиданий)

Загружается «Rhythm Changes (демо)»: 4/4, ключ B- (си минор), 160 BPM, Swing,
композитор «Gershwin / Ragas». Такты:
- 1–8: секция A. В такте аккорды на долях 1 и 3: 1: B-7 E-7; 2: A-7 D7; 3: G-7
  C7; 4: F7 (доля 4 — D7/A, слэш с басом A); 5: B-7 E-7; 6: A-7 D7; 7: G-7 C7;
  8: F7 B-7.
- 9–10: секция B: 9: F7 F7; 10: E7 E-7.
- 11: секция A, B-7 на доле 1.
- 12: N.C. (без аккордов).
Всего 12 тактов. Курсор = (такт, доля). В такт приходят на долю 1; другие доли
достижимы Alt+←/→ (слайс 12). На такте 4 первый аккорд F7 на доле 1, D7/A — на
доле 4 (слэш с басом A).

## Известные ограничения (сейчас так задумано, не баги)

- Правки адресуют аккорд на доле курсора; аккордов «на той же доле» нет — F2 /
  Ctrl+C/X / Del сообщают «Нет аккорда для редактирования» (пустая доля или
  такт N.C.). Структура (метка/повтор/N.C.) на пустой доле правится отдельно —
  Ctrl+Del, как п.37.
- Alt-шаг идёт по долям, но шаг стрелкой-без-Alt — по аккордам и пустым тактам
  (слайс 11): «встать» на пустую долю можно только Alt+←/→, а «на пустой такт
  середины» — только Ctrl+←/→.
- Повторы/вольты создаются только в одну ступень: пара `[`/`]` + одно окончание
  (V). Третьего и дальнейших окончаний нет — как и в python irealstudio.
- Аккорд вводится **текстом** в поле (без подбора по списку, как python-пикер).
- Метки Fine (буква F) нет — как и в python она скрыта.
- Создание цифровки (слайс 2) без шаблона даёт пустую прогрессию: навигация
  ходит по уже набранным тактам; чтобы «продлить» цифровку, вставляй аккорды
  правее конца — такт появится.

## Форма репорта о баге

Для каждого несовпадения пришли одним сообщением:
1. Номер пункта из этого файла.
2. Что нажал (какие клавиши/меню).
3. Что ожидал услышать (цитату из чеклиста).
4. Что услышал/увидел на самом деле (озвучку и, если видно, поведение окна).
5. Случилось ли это на свежезапущенной программе (после перезапуска exe).
